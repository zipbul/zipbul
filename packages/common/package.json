{
  "name": "@zipbul/common",
  "version": "0.3.0",
  "description": "Shared types, interfaces, decorators and base classes for Zipbul",
  "license": "MIT",
  "type": "module",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --packages external --minify-syntax --minify-whitespace && tsc -p tsconfig.build.json"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zipbul/zipbul",
    "directory": "packages/common"
  },
  "dependencies": {
    "@zipbul/result": "catalog:"
  },
  "devDependencies": {
    "@zipbul/core": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
