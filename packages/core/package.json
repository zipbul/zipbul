{
  "name": "@zipbul/core",
  "version": "0.1.1",
  "description": "DI container, application bootstrap and module system for Zipbul",
  "license": "MIT",
  "type": "module",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build index.ts --outdir dist --target bun --format esm --packages external --minify-syntax --minify-whitespace && tsc -p tsconfig.build.json"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zipbul/zipbul",
    "directory": "packages/core"
  },
  "dependencies": {
    "@zipbul/baker": "^0.1.0",
    "@zipbul/common": "workspace:*",
    "@zipbul/logger": "workspace:*",
    "dotenv": "^16.4.5",
    "exponential-backoff": "^3.1.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5"
  }
}
