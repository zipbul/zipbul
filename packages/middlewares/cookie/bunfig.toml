[test]
onlyFailures = true
coverage = true
coverageReporter = ["text", "lcov"]
coverageThreshold = 1.0
coveragePathIgnorePatterns = [
  "node_modules/**",
  "dist/**",
  "../../libs/result/**",
  "../../framework/**",
  "../../adapters/**"
]

[test.reporter]
dots = true
