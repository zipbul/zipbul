[test]
preload = ["./test/setup.ts"]
onlyFailures = true
coverage = true
coverageReporter = ["text", "lcov"]
coverageThreshold = 0.95
coveragePathIgnorePatterns = [
  "node_modules/**",
  "dist/**",
  "../../libs/shared/**",
  "../../libs/result/**"
]

[test.reporter]
dots = true