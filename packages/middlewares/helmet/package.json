{
  "name": "@zipbul/helmet",
  "version": "0.0.1",
  "description": "Standards-compliant security headers engine for Web Fetch APIs (CSP, HSTS, COOP/COEP, Permissions-Policy, etc.)",
  "license": "MIT",
  "author": "Junhyung Park (https://github.com/parkrevil)",
  "repository": {
    "type": "git",
    "url": "https://github.com/zipbul/zipbul",
    "directory": "packages/middlewares/helmet"
  },
  "bugs": {
    "url": "https://github.com/zipbul/zipbul/issues"
  },
  "homepage": "https://github.com/zipbul/zipbul/tree/main/packages/middlewares/helmet#readme",
  "keywords": [
    "helmet",
    "security",
    "security-headers",
    "csp",
    "content-security-policy",
    "hsts",
    "permissions-policy",
    "coop",
    "coep",
    "cors",
    "owasp",
    "http",
    "fetch",
    "web-api",
    "bun",
    "typescript",
    "zipbul"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "type": "module",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "provenance": true
  },
  "scripts": {
    "build": "bun build index.ts --outdir dist --target bun --format esm --packages external --production && tsc -p tsconfig.build.json",
    "test": "bun test",
    "coverage": "bun test --coverage"
  },
  "dependencies": {
    "@zipbul/result": "workspace:*"
  },
  "sideEffects": false,
  "devDependencies": {
    "@zipbul/http-adapter": "workspace:*"
  },
  "peerDependencies": {
    "@zipbul/http-adapter": "workspace:*"
  }
}
