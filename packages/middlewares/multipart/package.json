{
  "name": "@zipbul/multipart",
  "version": "0.1.1",
  "description": "Streaming multipart/form-data parser built on Bun-native APIs",
  "license": "MIT",
  "author": "Junhyung Park (https://github.com/parkrevil)",
  "repository": {
    "type": "git",
    "url": "https://github.com/zipbul/zipbul",
    "directory": "packages/middlewares/multipart"
  },
  "bugs": {
    "url": "https://github.com/zipbul/zipbul/issues"
  },
  "homepage": "https://github.com/zipbul/zipbul/tree/main/packages/middlewares/multipart#readme",
  "keywords": [
    "multipart",
    "form-data",
    "file-upload",
    "streaming",
    "bun",
    "typescript",
    "zipbul"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "type": "module",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build index.ts --outdir dist --target bun --format esm --packages external --production && tsc -p tsconfig.build.json",
    "test": "bun test",
    "coverage": "bun test --coverage",
    "bench": "bun run bench/multipart.bench.ts"
  },
  "dependencies": {
    "@zipbul/result": "workspace:*",
    "@zipbul/shared": "workspace:*"
  },
  "devDependencies": {
    "mitata": "catalog:"
  },
  "sideEffects": false,
  "publishConfig": {
    "provenance": true
  }
}
