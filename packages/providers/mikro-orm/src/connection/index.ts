export { ConnectionRegistry } from './connection-registry';
export { ConnectionNotRegisteredError } from './errors';
export { DEFAULT_CONNECTION } from './constants';
export type { ConnectionName } from './types';
